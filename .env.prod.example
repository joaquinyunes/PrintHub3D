# Copiá este archivo a .env.prod y completá los valores.
# docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build

# --- Obligatorios ---
JWT_SECRET=cambiá-esto-por-una-cadena-larga-y-aleatoria
CLIENT_URL=https://tu-dominio.com
API_PUBLIC_URL=https://api.tu-dominio.com
NEXT_PUBLIC_API_URL=https://api.tu-dominio.com

# --- Puertos publicados en el host ---
FRONTEND_PORT=3000
BACKEND_PORT=5000

# --- Opcionales ---
DEFAULT_TENANT_ID=global3d_hq
REQUIRE_EMAIL_VERIFICATION=false
RATE_LIMIT_MAX=150
REDIS_URL=

# Pagos
MP_ACCESS_TOKEN=
MP_WEBHOOK_SECRET=

# WhatsApp (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
ADMIN_WHATSAPP=

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Global 3D <noreply@tu-dominio.com>"
